{% raw %}
name: Docs

on:
  pull_request:
    paths:
      - "**.md"
      - "src/**/*.php"
      - "tests/**/*.php"
      - ".github/workflows/docs.yml"
      - "lychee.toml"
  push:
    branches: [ main ]
    paths:
      - "**.md"
      - "src/**/*.php"
      - "tests/**/*.php"
      - ".github/workflows/docs.yml"
      - "lychee.toml"
  schedule:
    - cron: "23 5 * * 1" # weekly, off the hour
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  links:
    name: Check Links
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run Lychee
        uses: lycheeverse/lychee-action@e7477775783ea5526144ba13e8db5eec57747ce8 # v2.9.0
        with:
          args: "--verbose --no-progress './*.md' './src/**/*.php' './tests/**/*.php'"
          output: ${{ runner.temp }}/lychee/out.md
          format: markdown
          jobSummary: true
          fail: true

{% endraw %}
